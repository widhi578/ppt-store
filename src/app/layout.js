import './globals.css'

export const metadata = { title: 'PPTera — Premium Presentation Templates', description: 'Template PowerPoint premium untuk cerita yang lebih meyakinkan.' }

export default function RootLayout({ children }) { return <html lang="id"><body>{children}</body></html> }
